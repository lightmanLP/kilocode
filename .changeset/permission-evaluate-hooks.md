---
"kilo-code": minor
---

Add permission.evaluate.before and permission.evaluate.after plugin hooks so plugins can enrich permission metadata, optionally short-circuit early decisions, and override the final decision without changing the default allow, deny, and ask behavior when they do not intervene.
